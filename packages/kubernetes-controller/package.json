{
  "name": "@clavis/kubernetes-controller",
  "version": "0.1.0",
  "description": "Kubernetes import/export controller for Clavis secrets.",
  "license": "Apache-2.0",
  "packageManager": "yarn@4.10.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "clavis-controller": "dist/main.js"
  },
  "files": [
    "dist",
    "config"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/index.test.js"
  },
  "dependencies": {
    "@clavis/client": "workspace:*",
    "@kubernetes/client-node": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
