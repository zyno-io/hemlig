{
  "name": "@hemlig/pulumi-provider",
  "version": "0.1.0",
  "description": "Pulumi dynamic provider for Hemlig secret control-plane resources.",
  "license": "Apache-2.0",
  "packageManager": "yarn@4.10.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/index.test.js"
  },
  "peerDependencies": {
    "@pulumi/pulumi": "^3.166.0"
  },
  "dependencies": {
    "@hemlig/client": "workspace:*"
  },
  "devDependencies": {
    "@pulumi/pulumi": "^3.166.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
